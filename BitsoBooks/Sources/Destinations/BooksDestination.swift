import Foundation

/// Holds all the publicly available destinations from the `Books` module.
public enum BooksDestination: Hashable {
    /// Navigate to the list of all available books.
    case booksList
}
