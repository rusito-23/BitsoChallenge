import Foundation

/// Holds all the internal destinations from `Books` module.
struct Destinations {

    /// Navigate to the details of a particular book.
    // static func bookDetail() -> Destination {}
}
